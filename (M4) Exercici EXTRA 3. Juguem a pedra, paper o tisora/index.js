let randomNum = Math.floor(Math.random() * 3) + 1
let userNum = Number(document.getElementById("selection-dropdown").value)
let randomObject = document.getElementsByName(randomNum.toString())
let userObject = document.getElementsById("selection-dropdown").name
let result

const play = (selectionNum, notSelectedNum) => {

    selectionNum = userNum
    notSelectedNum = randomNum

    switch (true) {
        case (selectionNum === 1 && notSelectedNum === 1):
            result = 

    }

}